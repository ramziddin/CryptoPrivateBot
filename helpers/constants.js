const CURRENCIES = ["BTC", "TON", "USDT", "USDC", "BUSD"]

const MAINNET = "pay.crypt.bot" // @CryptoBot
const TESTNET = "testnet-pay.crypt.bot" // @CryptoTestnetBot

module.exports = {
  CURRENCIES,
  MAINNET,
  TESTNET,
}
